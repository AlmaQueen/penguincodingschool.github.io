import java.util.ArrayList;
import java.util.Scanner;
class Numbers{
public static void main (String[]args) {
Scanner scanner = new Scanner (System.in);
ArrayList<Integer> counting = new ArrayList<>();


for (int i=0; i<10; i++){
		int x = (int)(Math.random()*25);
counting.add(x);
}
System.out.println(counting+"Choose any two numbers that you would like on the list by typing them in.(seperated by a space)");
counting.add(13);
counting.add(46);
for {if (counting.get(i) %2==1)};

//Math.random()*25;

}
}